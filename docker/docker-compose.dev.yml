services:
  rabbitmq:
    image: rabbitmq:alpine
    ports:
      - 5672:5672

  orbitsphere-database:
    image: postgres:alpine
    restart: always
    env_file:
      - ../.env
    ports:
      - 5432:5432
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
